import React from "react";

const Loading = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
      }}
    >
      <h1>Loading...</h1>
    </div>
  );
};

export default Loading;
